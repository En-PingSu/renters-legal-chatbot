# MA Tenant Law Chatbot — requirements.txt
# Install with: pip install -r requirements.txt
# NOTE: install unsloth separately (see install_deps.bat) as it needs
#       a CUDA-version-specific wheel that pip can't resolve automatically.

# ── Core RAG pipeline ─────────────────────────────────────────────────────
chromadb
openai
python-dotenv
tiktoken

# ── Retrieval ─────────────────────────────────────────────────────────────
rank-bm25
PyStemmer

# ── Embeddings + cross-encoder reranker ──────────────────────────────────
sentence-transformers
# torch is pulled in by sentence-transformers; if CUDA version is wrong
# install manually: pip install torch --index-url https://download.pytorch.org/whl/cu124

# ── Scraping / document processing ───────────────────────────────────────
requests
beautifulsoup4
pdfplumber
markdownify

# ── Evaluation / charts ───────────────────────────────────────────────────
numpy
matplotlib

# ── Fine-tuning support libs ──────────────────────────────────────────────
# unsloth is NOT listed here — install separately, see install_deps.bat
datasets
trl
peft
accelerate
bitsandbytes
transformers
